
import React from 'react';

export const MenuLinkWrapper = ({children}) => {
    return(
        <div className="flex flex-wrap max-w-[180px]">
            {children}            
        </div>
    );
}
